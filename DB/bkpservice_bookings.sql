USE Service_Station
CREATE TABLE `service_bookings`(
    `booking_id` int(20),
    `cusomer_id` int(11),
    `first_name` varchar(50),
    `last_name` varchar(50),
    `mobile` varchar(15),
    `mobile_alt` varchar(15),
    `address` text,
    `bike_id` varchar(10),
    `ss_id` varchar(10),
    `is_free_service` int(1),
    `customer_notes` text,
    `coupon_id` varchar(11),
    `bike_running_km` int(2),
    `is_regular_service` int(1),
    `service_rating` float(4),
    `service_feedback` text,
    `job_card_number` varchar(30),
    `servicing_by` varchar(255),
    `bill_no` varchar(30),
    `bill_amount` int(8),
    `pending_work` text,
    `ss_recomendation` text,
    `current_service_status_id` int(2),
    `register_date` timestamp,
    `booking_date` timestamp,
    `delivery_date` timestamp,
    `updatedAt` timestamp,
    `createdAt` timestamp,
    PRIMARY KEY(`booking_id`)
)AUTO_INCREMENT 1;
